# Default: treat files as text, normalize to LF
* text=auto eol=lf

# Keep Windows-only files as CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Explicitly ensure shell scripts are LF
*.sh text eol=lf

# Keep our version of the README
README.md merge=ours
